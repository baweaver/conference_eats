<%= form_with(model: [@gathering, gathering_outing]) do |form| %>
  <% if gathering_outing.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gathering_outing.errors.count, "error") %> prohibited this gathering_outing from being saved:</h2>

      <ul>
        <% gathering_outing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
